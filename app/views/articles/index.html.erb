<h1>Blog Posts</h1>
<!--<p>-->
<!--    <%= link_to "Create new article", new_article_path %>-->
<!--</p>-->


<table>
   
    
    
    
    <% @articles.each do |l| %>
        <div class="well">
        <!--<td><%= l.title %></td>-->
        <!--<td><%= l.description %></td>-->
        	<h3><%= l.title%></h3>
        	<p><%= l.description %></p>
        <!--<td><%= link_to "Edit", edit_article_path(l) %></td>-->
        <!--<td><%= link_to "Show", article_path(l) %></td>-->
        <%= link_to "Show", article_path(l), :class => 'btn btn-default' %>
        <!--<%= link_to "Edit", edit_article_path(l), :class => 'btn btn-default' %>-->
        <!--<td><%= link_to "Delete", article_path(l), method: :delete, data: {confirm: "Are you sure?"} %></td>-->
        </div>
    <% end %>

</table>